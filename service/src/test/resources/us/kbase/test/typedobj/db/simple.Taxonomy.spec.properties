module Taxonomy {
    /* taxonomy identifier */
    typedef int taxonomy_id;

    typedef structure {
        taxonomy_id id;
        taxonomy_id parent_id;
        string name;
        list<taxonomy_id> sub_taxons;
    } taxon;
};
