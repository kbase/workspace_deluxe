module First {
    typedef int seq_id;

    typedef structure {
        int start;
        int stop;
        seq_id seq_id;
    } seq_pos;
    
    funcdef getPositions(seq_id seq) returns (list<seq_pos>);
};
