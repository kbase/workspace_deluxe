module Annotations {

	typedef structure {
		string name;
	} type1;

	/* @id ws Annotations.type1 */
	typedef string type1ref;

	/*
	New comment.
	@searchable ws_subset ref 
	*/
	typedef structure {
		type1ref ref;
	} type2;

	typedef structure {
		type2 val;
	} type3;
};