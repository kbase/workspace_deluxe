#include <SomeModule.types>
/*
Super large info about module DepModule!
There are many line in this comment so you'll probably never read some of them...
Another line
One more line
But anyway give a shot.
*/
module DepModule {
	/* New type */
	typedef structure {SomeModule.AType thing;} BType;
	/* Old function */
	funcdef old_call(SomeModule.AType param) returns (SomeModule.AType);
	/* New function */
	funcdef new_call(BType param) returns (BType) authentication required;
};
